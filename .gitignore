# Dependencies
node_modules/
bun.lockb

# Runtime data (contains tokens/secrets)
# These are in ~/.lark-dispatcher/ not in the repo,
# but just in case someone copies them here
config.json
sessions.json
daemon.pid
*.log
logs/

# Plugin caches
plugin-standalone/node_modules/
plugin-dispatcher/node_modules/
dispatcher/node_modules/

# Bun lock files in subdirs
plugin-standalone/bun.lock
plugin-dispatcher/bun.lock
dispatcher/bun.lock

# OS
.DS_Store

# IDE
.vscode/
.idea/
